"use client"

// import Image from "next/image"

export default function Home() {
  return (
    <>
      Home
      {/* <Image
        priority
        src="/cat.webp"
        alt="hello"
        width="500"
        height="500"
        className="my-6"
      /> */}
      {/* <div className="my-6">
        <div className="my-4 text-xl">Zustand</div>
        <h2>Bear count: {bears}</h2>
        <h2>Cat count: {cat}</h2>
        <div className="flex space-x-2 my-4">
          <Button
            onClick={() => {
              upBear(1)
            }}
            color="primary"
            startContent={<PlusCircleIcon className="w-6" />}
          >
            Bear 증가
          </Button>
          <Button
            onClick={() => {
              upCat(1)
            }}
            color="primary"
            startContent={<PlusCircleIcon className="w-6" />}
          >
            Cat 증가 (+Bear)
          </Button>
        </div>
      </div> */}
    </>
  )
}
